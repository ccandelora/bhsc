<%= form_for(@reservation_week) do |f| %>
  <% if @reservation_week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reservation_week.errors.count, "error") %> prohibited this reservation_week from being saved:</h2>

      <ul>
      <% @reservation_week.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :res_date %><br>
    <%= f.text_field :res_date %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
